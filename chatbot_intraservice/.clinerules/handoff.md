# Session handoff — `.clinerules`

> Статус на конец сессии 2026-09-07.

## Сделано

- Создан каталог `.clinerules/` с:
  - `rules.md` — главные правила работы через ИИ (в т.ч. слабые модели,
    план‑ориентированный подход, история решений, тесты).
  - `decisions.md` — лог архитектурных/тактических решений по подходу.
  - `handoff.md` — этот файл (сессионный статус).
- В `rules.md` зафиксированы:
  - fallback на `gpt-oss-120b` при «слишком длинный»/пустой ответе слабой модели;
  - план ‑ разложение задачи ‑ микрозадачи с микровалидацией после каждого шага;
  - обязательный `docs/handoff.md` / `docs/pipeline/решения.md` на конец сессии;
  - логирование `model_*` в `_analysis_*.json`.
- Проектные изменения отменены (см. `decisions.md`), `AGENTS.md` и `README.md`
  не изменены в этой сессии.

## Блокеры

- Нет.

## Следующие шаги

1. Применять `.clinerules/rules.md` ко всем новым задачам ИИ.
2. По окончании работы — обновить `docs/handoff.md` и
   `docs/pipeline/решения.md`.
3. При необходимости добавить запись в `.clinerules/decisions.md`.
