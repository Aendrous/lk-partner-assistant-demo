# Чатбот IntraService (1 линия HelpDesk). Установка из папки chatbot_intraservice:
#   python -m venv .venv && .\.venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
#
# Либо из корня монорепо: pip install -r ../requirements.txt && pip install -r requirements.txt

streamlit>=1.32.0
requests>=2.31.0
python-dotenv>=1.0.0
urllib3>=2.0.0
extract-msg>=0.48.0
pymupdf>=1.24.0
python-docx>=1.1.0
olefile>=0.46
